#include <stdio.h>
#include <math.h>

#define PI 3.14159265358979323846

int main()
{
    double theta_degrees = 45;

    double theta_radians = PI * theta_degrees / 180.0;

    double ratio_d_lambda = 1 / sin(theta_radians);

    printf("The ratio is: %.3f\n", ratio_d_lambda);

    return 0;
}
